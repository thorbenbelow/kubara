project_id             = "{{ .cluster.terraform.projectId }}"
credentials_group_name = "cg-{{ .cluster.name }}-{{ .cluster.stage }}"
bucket_name            = "bucket-tf-{{ .cluster.name }}-{{ .cluster.stage }}"
