# T Cloud Public Terraform modules

Provider-specific Terraform modules for T Cloud Public.

The first supported Kubernetes path is Cloud Container Engine (CCE). The modules intentionally keep network, keypair, KMS, and cluster lifecycle separate so generated examples can be used as-is or replaced with existing landing-zone resources.
