# Agent guide for this kubara platform repository

This repository is managed with **kubara** — an opinionated, GitOps-first CLI for bootstrapping and
operating Kubernetes platforms (a single Go binary). You are likely a coding agent (Claude Code,
Codex, …): this file is your token-lean entry point. Prefer kubara's own self-describing commands
over guessing, and open the linked docs only when you need depth.

- Installed kubara version: `{{ .Version }}`
- Authoritative command reference: run `kubara --help` and `kubara <command> --help`
- Authoritative config reference: run `kubara schema` (emits the JSON Schema for `config.yaml`)
- Rendered docs (latest): {{ .DocsSite }}
- Full docs index for this version: {{ .LlmsTxt }} (an `llms.txt` map of every page; fetch it to find docs beyond the curated list below)

## Core workflow (run in this order)

1. Make sure you are in a git repository that is either empty or already contains a `config.yaml`.
2. `kubara init` — generate `config.yaml` from `.env`.
3. `kubara generate --helm` (add `--terraform` if you provision infrastructure with kubara) — render
   Helm/Terraform artifacts from the service catalog into this repository.
4. `kubara bootstrap <cluster-name>` — install Argo CD onto the target cluster.
5. Commit & push the generated files. Argo CD reconciles everything else from Git.

Use `kubara generate --dry-run` to preview without writing.

## What lives in this repository

- `config.yaml` — the platform definition (clusters, stages, services). Run `kubara schema` for its
  JSON Schema instead of guessing field names.
- `.env` — inputs/credentials consumed by `kubara init`. Git-ignored; never commit it.
- `platform-configs/` — generated Helm values and Terraform overlays. Review them and fill
  in any environment-specific values before bootstrapping.

## Conventions for agents

- The CLI is fully non-interactive. Drive it with flags, the `.env` file, or `KUBARA_*` environment
  variables — never expect an interactive prompt.
- Reach for `kubara <command> --help` and `kubara schema` before inferring flags or config fields.
- Match the tool to the workload, and ask the user when it is unclear: use Argo CD
  `Application`s/`ApplicationSet`s for cluster-specific workloads, and the catalog for components
  that are part of the reusable platform (see the Catalogs doc — "when not to use a catalog").
- Keep changes GitOps-friendly: the desired state is the committed files, not live `kubectl` mutations.

## Documentation (Markdown, this version)

Read these only when the CLI's own help is not enough. Links point at the published Markdown on the
docs site (cheaper to read than the rendered HTML, and served from the docs site rather than
rate-limited raw GitHub); start at the top, treat "Reference" as load-on-demand.

- [Prerequisites]({{ .DocsBase }}/1_getting_started/prerequisites/index.md): tools and cloud resources to provision first
- [Bootstrapping]({{ .DocsBase }}/1_getting_started/bootstrapping/index.md): the end-to-end walkthrough
- [Quick start]({{ .DocsBase }}/1_getting_started/quick_start/index.md): local evaluation workflow
- [Core concepts]({{ .DocsBase }}/2_concepts/overview_core_concept/index.md): hub/spoke model and platform layout
- [Catalogs]({{ .DocsBase }}/2_concepts/catalogs/index.md): the service catalog that drives generation
- [Components]({{ .DocsBase }}/6_components/components_overview/index.md): the built-in platform components

### Reference (load on demand)

- [Add a spoke cluster]({{ .DocsBase }}/4_building_your_platform/add_spoke_cluster/index.md)
- [Workload onboarding]({{ .DocsBase }}/5_workload_onboarding/overview/index.md)
- [Architecture]({{ .DocsBase }}/7_architecture/architecture_overview/index.md)
- [FAQ]({{ .DocsBase }}/9_reference/faq/index.md) · [Known issues]({{ .DocsBase }}/9_reference/known_issues/index.md)

_Generated by `kubara agents`. Re-run `kubara agents --overwrite` to refresh after upgrading kubara._
