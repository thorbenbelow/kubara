[project]
name = "docs"
version = "0.1.0"
description = "Documentation for kubara"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "mike",
    "mkdocs",
    "mkdocs-material",
    "mkdocs-drawio",
]
